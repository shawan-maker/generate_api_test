● 方案已完成，包含三个 Phase 的详细修改清单和遗漏检查。核心思路：

  1. Stage 1 增强：_do_create / _do_generic_operation 记录更多已验证信息 → build_playbook() 完整传递到 playbook
  2. Stage 2 清理：10 个 _step_* 函数删除所有回退/探测逻辑，纯执行 playbook 提供的 locator
  3. 遗漏检查：覆盖了所有 10 个 step 函数 + _build_update_steps / _build_generic_steps + selector 跨上下文风险

● User approved Claude's plan
  ⎿  Plan saved to: C:\Users\29091\.claude\plans\happy-forging-noodle.md · /plan to edit
     Playbook 完整性修复方案：消除 Stage 2 所有探测行为

     Context
    
     核心原则：Stage 2 的职责是纯执行——根据 Stage 1 提供的 playbook 回放操作并捕获 API。Stage 2 不应做任何元素探测、DOM 状态检测、selector 回退。
    
     当前问题：Stage 1 输出的 playbook 信息不完整，导致 Stage 2 被迫在运行时做探测（遍历多个 selector、检测 DOM 状态、调用 KB XPath 模板匹配元素）。这些探测行为违反了 Stage 2
     的设计原则。
    
     解决方案：
     1. 增强 Stage 1 的 playbook 输出，补齐所有缺失信息
     2. 删除 Stage 2 中的所有探测逻辑，改为纯执行
     3. 确保 Stage 1 已经验证过的 locator/属性被完整传递到 Stage 2
    
     ---
     当前问题分析
    
     Stage 1 playbook 输出缺失清单
    
     通过分析现有 playbook（用户管理_playbook.json）和 Stage 2 代码，发现以下信息缺失：
    
     ┌────────────────────┬─────────────────────────────────────────────────────────────────────────────────┬──────────────────────────────────────────────────────────────┐
     │      字段类型      │                                    缺失信息                                     │                     Stage 2 被迫做的探测                     │
     ├────────────────────┼─────────────────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────┤
     │ el-select 字段     │ kb_category（空字符串）option_text（要选择的选项文本）is_editable（是否可搜索） │ 从 type 推断 kb_category运行时读取下拉选项DOM 检测是否可搜索 │
     ├────────────────────┼─────────────────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────┤
     │ el-cascader 字段   │ kb_categoryoptions_path（完整路径如 ["一级", "二级"]）                          │ KB XPath 展开逐级发现选项                                    │
     ├────────────────────┼─────────────────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────┤
     │ date-picker 字段   │ kb_category选择策略 locator                                                     │ KB XPath 展开A/B 回退尝试                                    │
     ├────────────────────┼─────────────────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────┤
     │ form-checkbox 字段 │ kb_category精确选项 locator                                                     │ KB XPath 遍历fallback 链                                     │
     ├────────────────────┼─────────────────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────┤
     │ radio 字段         │ kb_category已验证的选项 locator                                                 │ 构造多个备选 locator检查 DOM 存在性                          │
     ├────────────────────┼─────────────────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────┤
     │ 按钮步骤           │ 仅文本，无已验证的精确 locator                                                  │ 遍历文本变体（空格处理）5个通用选择器回退                    │
     ├────────────────────┼─────────────────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────┤
     │ dialog/wait 步骤   │ interaction_mode（dialog/page-nav）精确容器 locator                             │ 检测 DOM 状态遍历6个通用容器选择器                           │
     ├────────────────────┼─────────────────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────┤
     │ confirm 步骤       │ 仅 dialog_type，无精确 locator                                                  │ 调用通用 confirm_dialog() 回退                               │
     ├────────────────────┼─────────────────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────┤
     │ hover 步骤         │ trigger_method（hover/click）                                                   │ hover 失败后回退到 clickDOM 验证展开状态                     │
     ├────────────────────┼─────────────────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────┤
     │ assert_success     │ 硬编码默认值 .el-message--success                                               │ 无精确 locator 时静默失败                                    │
     ├────────────────────┼─────────────────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────┤
     │ checkbox 步骤      │ 无 selector                                                                     │ 硬编码 .el-checkbox__input                                   │
     └────────────────────┴─────────────────────────────────────────────────────────────────────────────────┴──────────────────────────────────────────────────────────────┘
    
     ---
     修改方案
    
     Phase 1: Stage 1 增强 — 补齐 playbook 输出信息
    
     1.1 修改 _do_create 返回值（discover_ui.py:1778-1926）
    
     当前输出：
     {
         "success": True,
         "fill_data": {...},
         "fill_rules": {...},
         "form_fields": [...],  # 包含 kb_category 但后续 build_playbook 未传递
         "marker": "...",
         "trigger_locator": "button:has-text('创建用户')",
         "submit_locator": ".el-dialog__footer button:has-text('确定')",
         "dialog_locator": ".el-dialog__wrapper",
         "success_locator": ".el-message--success",
     }
    
     需要新增的字段：
     {
         # ... 现有字段 ...
    
         # 交互模式（Stage 1 已知）
         "interaction_mode": "dialog" or "page-nav",  # 从 dialog_detected 推断
    
         # 为每个 multi_step 字段记录已验证的属性
         "multi_step_field_details": [
             {
                 "label": "系统角色",
                 "kb_category": "el-select",
                 "selector": "div > .el-form-item:nth-of-type(9) .el-select",
                 "is_editable": True,  # Stage 1 已检测
                 "option_text": "普通用户",  # Stage 1 已读取选项
                 "fill_rule": {...}
             },
             {
                 "label": "部门角色",
                 "kb_category": "el-select",
                 "selector": "...",
                 "is_editable": False,
                 "option_text": "",  # 空表示选第一项
                 "fill_rule": {...}
             }
         ],
    
         # radio 字段的已验证选项 locator
         "radio_field_details": [
             {
                 "label": "性别",
                 "kb_category": "radio",
                 "option_locator": ".el-radio:nth-child(1) .el-radio__input",  # 已验证
                 "option_text": "男",
                 "fill_rule": {...}
             }
         ],
    
         # 按钮的精确 locator（含空格变体处理）
         "trigger_locator_verified": "button:has-text('创 建 用 户')",  # Stage 1 实际点击成功的 locator
         "submit_locator_verified": ".el-dialog__footer button:has-text('确 定')",
     }
    
     修改逻辑：
     - 在 _do_create 中，fill_multi_step_fields 执行后，记录每个 multi_step 字段的实际执行情况（是否成功、选择的选项文本）
     - 在 fill_create_form 中，记录 radio 字段的实际点击 locator
     - 记录实际点击成功的 button locator（包括空格变体）
    
     1.2 修改 _do_generic_operation 返回值（discover_ui.py:2258-2414）
    
     当前输出：
     {
         "success": True,
         "selectors": {"trigger": "冻结", "row_selector": "..."},
         "is_dropdown_operation": True,
         "dropdown_parent_selector": ".el-dropdown",
         "dropdown_item_locator": ".el-dropdown-menu__item:has-text('冻结')",
         "confirm_dialog_type": "message-box",
         "confirm_locator": ".el-message-box__btns button:has-text('确定')",
     }
    
     需要新增的字段：
     {
         # ... 现有字段 ...
    
         # dropdown 触发方式
         "dropdown_trigger_method": "hover" or "click",  # Stage 1 已验证哪种方式有效
    
         # 精确的 checkbox locator（如果有）
         "checkbox_locator": ".el-checkbox__input",  # 或 Ant Design 的对应类名
     }
    
     1.3 修改 build_playbook() 函数（discover_ui.py:2910-3273）
    
     _build_create_steps 增强（第 2977-3061 行）：
    
     当前问题：
     - field_info 没有传递 kb_category
     - 没有传递 multi_step 字段的 is_editable、option_text
     - 没有传递 radio 字段的 option_locator
     - trigger_locator 和 submit_locator 是构造的，不是 Stage 1 实际验证的
    
     修改方案：
     def _build_create_steps(op_data: dict) -> list:
         steps = []
    
         # Step 1: 点击创建按钮（使用已验证的 locator）
         trigger_locator = op_data.get("trigger_locator_verified") or op_data.get("trigger_locator")
         if trigger_locator:
             steps.append({
                 "action": "click_button",
                 "playwright_locator": trigger_locator,
                 "description": "点击创建按钮"
             })
    
         # Step 2: 等待对话框（添加 interaction_mode）
         interaction_mode = op_data.get("interaction_mode", "dialog")
         dialog_locator = op_data.get("dialog_locator", ".el-dialog__wrapper")
         steps.append({
             "action": "wait_for_dialog",
             "playwright_locator": dialog_locator,
             "interaction_mode": interaction_mode,  # 新增
             "description": "等待创建对话框"
         })
    
         # Step 3: 填充表单
         form_fields = op_data.get("form_fields", [])
         fill_rules = op_data.get("fill_rules", {})
         multi_step_details = op_data.get("multi_step_field_details", [])
         radio_details = op_data.get("radio_field_details", [])
    
         if form_fields and fill_rules:
             fields_info = []
    
             # 处理普通字段（input/textarea）
             for field in form_fields:
                 label = field.get("label", "")
                 selector = field.get("selector")
                 field_type = field.get("type", "input")
                 kb_category = field.get("kb_category", "")
    
                 if not selector:
                     continue
    
                 # 跳过 multi_step 和 radio（单独处理）
                 if kb_category in ("el-select", "el-cascader", "date-picker", "form-checkbox", "radio"):
                     continue
    
                 rule = fill_rules.get(label, {})
                 field_info = {
                     "label": label,
                     "playwright_locator": selector,
                     "type": field_type,
                     "kb_category": kb_category,
                     "fill_rule": rule
                 }
    
                 if label in ("名称", "账号", "name", "username", "userName"):
                     field_info["is_marker"] = True
    
                 fields_info.append(field_info)
    
             # 添加 multi_step 字段（带完整属性）
             for ms_field in multi_step_details:
                 fields_info.append({
                     "label": ms_field["label"],
                     "playwright_locator": ms_field["selector"],
                     "type": ms_field["kb_category"].replace("el-", ""),  # el-select -> select
                     "kb_category": ms_field["kb_category"],
                     "fill_rule": ms_field["fill_rule"],
                     "is_editable": ms_field.get("is_editable", False),
                     "option_text": ms_field.get("option_text", ""),
                 })
    
             # 添加 radio 字段（带已验证的选项 locator）
             for radio_field in radio_details:
                 fields_info.append({
                     "label": radio_field["label"],
                     "playwright_locator": radio_field["option_locator"],
                     "type": "radio",
                     "kb_category": "radio",
                     "fill_rule": radio_field["fill_rule"],
                     "option_text": radio_field.get("option_text", ""),
                 })
    
             if fields_info:
                 steps.append({
                     "action": "fill_form",
                     "fields": fields_info,
                     "description": "填充表单字段"
                 })
    
         # Step 4: 点击提交按钮（使用已验证的 locator）
         submit_locator = op_data.get("submit_locator_verified") or op_data.get("submit_locator")
         submit_text = op_data.get("submit_text") or op_data.get("selectors", {}).get("submit")
         if submit_locator:
             steps.append({
                 "action": "click_button",
                 "playwright_locator": submit_locator,
                 "text": submit_text,
                 "description": "点击提交按钮"
             })
    
         # Step 5: 验证成功
         success_locator = op_data.get("success_locator", ".el-message--success")
         steps.append({
             "action": "assert_success",
             "playwright_locator": success_locator,
             "description": "验证创建成功"
         })
    
         return steps
    
     _build_dropdown_steps 增强（第 3064-3115 行）：
    
     def _build_dropdown_steps(op_data: dict) -> list:
         steps = []
    
         # Step 1: 定位数据行
         steps.append({
             "action": "find_row",
             "description": "定位目标数据行"
         })
    
         # Step 2: 展开下拉菜单（添加 trigger_method）
         if op_data.get("is_dropdown_operation"):
             parent_selector = op_data.get("dropdown_parent_selector", ".el-dropdown")
             trigger_method = op_data.get("dropdown_trigger_method", "hover")  # 新增
             steps.append({
                 "action": "hover_dropdown",
                 "parent_selector": parent_selector,
                 "trigger_method": trigger_method,  # 新增
                 "description": "展开更多菜单"
             })
    
             # Step 3: 点击下拉项
             item_locator = op_data.get("dropdown_item_locator")
             if item_locator:
                 steps.append({
                     "action": "click_dropdown_item",
                     "playwright_locator": item_locator,
                     "description": f"点击下拉菜单项"
                 })
    
         # Step 4: 处理确认对话框（确保有 confirm_locator）
         if op_data.get("confirm"):
             dialog_type = op_data.get("confirm_dialog_type", "message-box")
             confirm_locator = op_data.get("confirm_locator")
    
             if not confirm_locator:
                 # Stage 1 应该已经记录，这里不应该发生
                 LOG.warning(f"Stage 1 未提供 confirm_locator，使用通用回退")
                 confirm_locator = ".el-message-box__btns button:has-text('确定')"
    
             steps.append({
                 "action": "click_confirm_dialog",
                 "dialog_type": dialog_type,
                 "confirm_locator": confirm_locator,  # 必须有值
                 "description": "点击确认对话框"
             })
    
         # Step 5: 验证成功
         success_locator = op_data.get("success_locator", ".el-message--success")
         steps.append({
             "action": "assert_success",
             "playwright_locator": success_locator,
             "description": "验证操作成功"
         })
    
         return steps
    
     _build_delete_steps 增强（第 3118-3168 行）：
    
     def _build_delete_steps(op_data: dict) -> list:
         steps = []
    
         # Step 1: 定位数据行
         steps.append({
             "action": "find_row",
             "description": "定位目标数据行"
         })
    
         # Step 2: 勾选 checkbox（添加精确 locator）
         selectors = op_data.get("selectors", {})
         if selectors.get("row_selector"):
             checkbox_locator = op_data.get("checkbox_locator", ".el-checkbox__input")
             steps.append({
                 "action": "select_row_checkbox",
                 "checkbox_locator": checkbox_locator,  # 新增
                 "description": "勾选行 checkbox"
             })
    
         # Step 3: 点击删除按钮
         trigger = selectors.get("trigger")
         trigger_locator = op_data.get("trigger_locator_verified") or f"button:has-text('{trigger}')"
         if trigger:
             steps.append({
                 "action": "click_button",
                 "text": trigger,
                 "playwright_locator": trigger_locator,
                 "description": "点击删除按钮"
             })
    
         # Step 4: 处理确认对话框
         if op_data.get("confirm"):
             dialog_type = op_data.get("confirm_dialog_type", "message-box")
             confirm_locator = op_data.get("confirm_locator", ".el-message-box__btns button:has-text('确定')")
             steps.append({
                 "action": "click_confirm_dialog",
                 "dialog_type": dialog_type,
                 "confirm_locator": confirm_locator,
                 "description": "点击确认对话框"
             })
    
         # Step 5: 验证行消失
         steps.append({
             "action": "assert_row_disappeared",
             "description": "验证数据行已消失"
         })
    
         return steps
    
     1.4 修改 fill_multi_step_fields 和 fill_create_form（form_filler.py）
    
     fill_multi_step_fields 增强（第 155-195 行）：
    
     记录每个 multi_step 字段的实际执行情况：
     async def fill_multi_step_fields(self, fields: List[Dict], framework: str = "element-ui") -> tuple[int, List[Dict]]:
         """处理 multi_step 类型的表单字段。
    
         Returns:
             (成功处理数, 详细信息列表)
         """
         from . import const
    
         executor = self._get_executor(framework)
         filled = 0
         details = []
    
         for field in fields:
             kb_cat = field.get("kb_category", "")
             if kb_cat not in const.MULTI_STEP_TYPES and kb_cat != "form-checkbox":
                 continue
    
             label = field.get("label", "")
             selector = field.get("selector") or field.get("playwright_locator", "")
    
             # 执行并记录详细信息
             success, detail = await executor.execute_with_details(kb_cat, label, option_text="", selector=selector)
    
             if success:
                 filled += 1
                 details.append({
                     "label": label,
                     "kb_category": kb_cat,
                     "selector": selector,
                     "is_editable": detail.get("is_editable", False),
                     "option_text": detail.get("option_text", ""),
                 })
                 LOG.info(f"    ✅ multi_step: {label} ({kb_cat})")
             else:
                 LOG.warning(f"    ⚠️ multi_step 失败: {label} ({kb_cat})")
    
         return filled, details
    
     MultiStepExecutor.execute_with_details 新增方法（form_filler.py）：
    
     async def execute_with_details(self, kb_category: str, label: str,
                                    option_text: str = "", options: list = None,
                                    selector: str = "") -> tuple[bool, dict]:
         """执行 multi_step 操作并返回详细信息。
    
         Returns:
             (success, detail_dict)
             detail_dict 包含 is_editable、option_text 等 Stage 2 需要的信息
         """
         handlers = {
             "el-select": self._execute_select_with_details,
             # ... 其他 handlers
         }
         handler = handlers.get(kb_category)
         if not handler:
             return False, {}
    
         try:
             return await handler(label, option_text, options, selector)
         except Exception as e:
             LOG.warning(f"MultiStepExecutor: {kb_category}({label}) 失败: {e}")
             return False, {}
    
     async def _execute_select_with_details(self, label: str, option_text: str,
                                            _options, selector: str = "") -> tuple[bool, dict]:
         """el-select 多步交互，返回详细信息。"""
         steps = self.kb.get_steps("el-select", self.framework)
         if not steps:
             return False, {}
    
         detail = {"is_editable": False, "option_text": ""}
    
         # Step 1: expand
         expanded = False
         if selector:
             expanded = await self._expand_by_selector(selector, label)
         if not expanded:
             expanded = await self._try_step_patterns(steps.get("expand", {}), label=label)
         if not expanded:
             return False, detail
         await self.page.wait_for_timeout(500)
    
         # Step 1.5: 自动发现选项
         if not option_text:
             option_text = await self._discover_first_option()
         detail["option_text"] = option_text
    
         # Step 2: editable-check
         editable_patterns = steps.get("editable-check", {}).get("patterns", [])
         is_editable = await self._check_element_visible(editable_patterns, label=label)
         detail["is_editable"] = is_editable
    
         # Step 3: 选择
         if is_editable and option_text:
             await self._try_step_patterns(steps.get("fill", {}), label=label)
             await self.page.wait_for_timeout(300)
             selected = await self._try_step_patterns(
                 steps.get("select", {}), label=label, option_text=option_text)
         else:
             selected = await self._try_step_patterns(
                 steps.get("first-option", {}), label=label)
    
         return selected, detail
    
     Phase 2: Stage 2 清理 — 删除所有探测逻辑
    
     2.1 修改 _step_click_button（capture_apis.py:275-328）
    
     删除所有回退逻辑：
     async def _step_click_button(page, step: dict, button_driver: ButtonDriver, ctx: dict):
         """步骤：点击按钮（纯执行，不做回退）"""
         locator = step.get("playwright_locator")
         text = step.get("text")
    
         if not locator:
             raise Exception("click_button 步骤缺少 playwright_locator（Stage 1 未提供）")
    
         # 直接使用 Stage 1 提供的已验证 locator
         await page.click(locator, timeout=5000)
    
         # 不再做任何回退尝试
    
     删除的代码：
     - 第 286-289 行：except: if text: LOG.debug("回退到文本匹配")
     - 第 294-327 行：所有文本变体生成和通用选择器回退逻辑
    
     2.2 修改 _step_wait_for_dialog（capture_apis.py:331-390）
    
     删除所有 DOM 探测：
     async def _step_wait_for_dialog(page, step: dict, ctx: dict):
         """步骤：等待表单环境就绪（纯执行，不做探测）"""
         locator = step.get("playwright_locator")
         interaction_mode = step.get("interaction_mode", "dialog")
         timeout = step.get("timeout_ms", 5000)
    
         if not locator:
             raise Exception("wait_for_dialog 步骤缺少 playwright_locator（Stage 1 未提供）")
    
         ctx["interaction_mode"] = interaction_mode
    
         if interaction_mode == "dialog":
             # Dialog 模式：等待对话框出现
             await page.wait_for_selector(locator, state="visible", timeout=timeout)
             await page.wait_for_timeout(1000)
         elif interaction_mode == "page-nav":
             # Page-nav 模式：等待页面加载完成
             await page.wait_for_load_state("networkidle", timeout=10000)
             await page.wait_for_timeout(1000)
         else:
             raise Exception(f"未知的 interaction_mode: {interaction_mode}")
    
         # 不再做任何 DOM 状态检测或回退选择器遍历
    
     删除的代码：
     - 第 351-364 行：URL 变化检测和 page.evaluate DOM 扫描
     - 第 367-378 行：fallback_selectors 遍历
     - 第 381-388 行：page.evaluate 检查表单元素
    
     2.3 修改 _step_fill_form（capture_apis.py:393-503）
    
     删除 type 推断和 radio 探测：
     async def _step_fill_form(page, step: dict, button_driver: ButtonDriver) -> str | None:
         """步骤：填充表单字段（纯执行，不做推断）"""
         from .form_filler import apply_fill_rule, FormFiller
         from . import const
    
         fields = step.get("fields", [])
         filled_count = 0
         marker_value = None
    
         # 收集字段
         multi_step_fields = []
         simple_fields = []
    
         for field in fields:
             label = field.get("label")
             kb_category = field.get("kb_category", "")
             locator = field.get("playwright_locator")
    
             if not locator:
                 LOG.warning(f"    字段 {label} 缺少 locator，跳过")
                 continue
    
             # Stage 1 必须提供 kb_category，不做推断
             if not kb_category:
                 LOG.warning(f"    字段 {label} 缺少 kb_category（Stage 1 未提供），跳过")
                 continue
    
             if kb_category in const.MULTI_STEP_TYPES or kb_category == "form-checkbox":
                 multi_step_fields.append(field)
             else:
                 simple_fields.append(field)
    
         # 处理普通字段
         for field in simple_fields:
             label = field.get("label")
             locator = field.get("playwright_locator")
             fill_rule = field.get("fill_rule")
             is_marker = field.get("is_marker", False)
             kb_category = field.get("kb_category", "")
    
             # Radio 字段：Stage 1 已提供精确的 option_locator
             if kb_category == "radio":
                 try:
                     await page.click(locator, timeout=3000)
                     filled_count += 1
                     LOG.debug(f"      选择 radio: {label}")
                 except Exception as e:
                     LOG.debug(f"      radio 失败: {label}: {e}")
                 continue
    
             # 普通 input/textarea
             if fill_rule:
                 value = apply_fill_rule(fill_rule)
             else:
                 LOG.warning(f"    字段 {label} 缺少 fill_rule，跳过")
                 continue
    
             if value is None:
                 continue
    
             try:
                 await page.fill(locator, str(value), timeout=3000)
                 filled_count += 1
                 LOG.debug(f"      填充: {label} = {value}")
                 if is_marker and not marker_value:
                     marker_value = value
             except Exception as e:
                 LOG.debug(f"      填充失败: {label}: {e}")
    
         # 处理 multi_step 字段
         if multi_step_fields:
             ff = FormFiller(page)
             framework = step.get("framework", "element-ui")
             ms_filled = await ff.fill_multi_step_fields(multi_step_fields, framework)
             filled_count += ms_filled
             LOG.debug(f"    multi_step 完成: {ms_filled}/{len(multi_step_fields)}")
    
         LOG.info(f"    已填充 {filled_count}/{len(fields)} 个字段")
         return marker_value
    
     删除的代码：
     - 第 417-423 行：_TYPE_TO_KB 推断表
     - 第 438-441 行：if not kb_category and field_type in _TYPE_TO_KB 推断逻辑
     - 第 443-458 行：Radio 的多 selector 探测逻辑
    
     2.4 修改 _step_hover_dropdown（capture_apis.py:516-538）
    
     删除 hover/click 回退：
     async def _step_hover_dropdown(page, step: dict, button_driver: ButtonDriver):
         """步骤：展开下拉菜单（纯执行，不做回退）"""
         parent_selector = step.get("parent_selector")
         trigger_method = step.get("trigger_method", "hover")
         wait_ms = step.get("wait_ms", 600)
    
         if not parent_selector:
             raise Exception("hover_dropdown 步骤缺少 parent_selector（Stage 1 未提供）")
    
         more_btn = await page.query_selector(parent_selector)
         if not more_btn:
             raise Exception(f"未找到 dropdown 触发器: {parent_selector}")
    
         # 使用 Stage 1 验证的触发方式
         if trigger_method == "hover":
             await more_btn.hover()
         else:
             await more_btn.click()
    
         await page.wait_for_timeout(wait_ms)
    
         # 不再做 DOM 展开验证或 hover/click 回退
    
     删除的代码：
     - 第 518 行：默认值 ".el-dropdown"
     - 第 530-538 行：DOM 展开验证和 hover→click 回退
    
     2.5 修改 _step_click_confirm_dialog（capture_apis.py:550-562）
    
     删除通用回退：
     async def _step_click_confirm_dialog(page, step: dict):
         """步骤：点击确认对话框（纯执行，不做回退）"""
         confirm_locator = step.get("confirm_locator")
    
         if not confirm_locator:
             raise Exception("click_confirm_dialog 步骤缺少 confirm_locator（Stage 1 未提供）")
    
         # 直接使用 Stage 1 提供的已验证 locator
         await page.wait_for_selector(confirm_locator, state="visible", timeout=3000)
         await page.click(confirm_locator)
    
         # 不再调用通用的 confirm_dialog() 回退
    
     删除的代码：
     - 第 559-562 行：else: from .button_driver import confirm_dialog; await confirm_dialog(page)
    
     2.6 修改 _step_select_row_checkbox（capture_apis.py:589-603）
    
     使用精确 selector：
     async def _step_select_row_checkbox(page, step: dict, button_driver: ButtonDriver, marker: str):
         """步骤：勾选数据行的 checkbox（使用精确 locator）"""
         if not marker:
             raise Exception("select_row_checkbox 步骤需要 marker")
    
         checkbox_locator = step.get("checkbox_locator")
         if not checkbox_locator:
             raise Exception("select_row_checkbox 步骤缺少 checkbox_locator（Stage 1 未提供）")
    
         row = await button_driver.find_data_row(marker)
         if not row:
             raise Exception(f"未找到数据行: {marker}")
    
         checkbox = await row.query_selector(checkbox_locator)
         if not checkbox:
             raise Exception(f"未找到 checkbox: {checkbox_locator}")
    
         await checkbox.click()
         await page.wait_for_timeout(300)
    
     删除的代码：
     - 第 598 行：硬编码 ".el-checkbox__input"
    
     2.7 修改 MultiStepExecutor._execute_select（form_filler.py:1214-1274）
    
     保留 selector 优先，但删除 KB XPath 回退：
     async def _execute_select(self, label: str, option_text: str, _options, selector: str = "") -> bool:
         """el-select 多步交互：使用 Stage 1 提供的 selector 展开"""
         steps = self.kb.get_steps("el-select", self.framework)
         if not steps:
             return False
    
         # Step 1: expand（必须使用 selector，不做 KB XPath 回退）
         if not selector:
             LOG.warning(f"    el-select {label} 缺少 selector（Stage 1 未提供）")
             return False
    
         expanded = await self._expand_by_selector(selector, label)
         if not expanded:
             return False
         await self.page.wait_for_timeout(500)
    
         # Step 1.5: 使用 Stage 1 提供的 option_text
         if not option_text:
             option_text = await self._discover_first_option()
    
         # Step 2: editable-check（使用 Stage 1 提供的 is_editable，不做 DOM 检测）
         # 注意：is_editable 应该从 field 参数传入，这里暂时保留 DOM 检测作为过渡
         editable_patterns = steps.get("editable-check", {}).get("patterns", [])
         is_editable = await self._check_element_visible(editable_patterns, label=label)
    
         # Step 3: 选择
         if is_editable and option_text:
             await self._try_step_patterns(steps.get("fill", {}), label=label)
             await self.page.wait_for_timeout(300)
             selected = await self._try_step_patterns(
                 steps.get("select", {}), label=label, option_text=option_text)
         else:
             selected = await self._try_step_patterns(
                 steps.get("first-option", {}), label=label)
    
         return selected
    
     删除的代码：
     - 第 1227-1228 行：KB XPath 回退 if not expanded: expanded = await self._try_step_patterns(...)
    
     注意：editable-check 和 fill/select/first-option 步骤仍然使用 KB XPath，因为这些是选择下拉选项的标准流程，不是探测。真正的探测是"展开下拉框"这一步。
    
     Phase 3: 验证和测试
    
     3.1 重新运行 Stage 1
    
     python -m module_discovery.run --project ecm-compute --module 用户管理 --stage 1
    
     验证 playbook 输出包含所有新增字段：
     - interaction_mode
     - multi_step_field_details（含 is_editable、option_text）
     - radio_field_details（含 option_locator）
     - trigger_locator_verified、submit_locator_verified
     - dropdown_trigger_method
     - confirm_locator（所有 confirm 步骤）
     - checkbox_locator
    
     3.2 运行 Stage 2
    
     python -m module_discovery.run --project ecm-compute --module 用户管理 --stage 2
    
     验证：
     - 无 "回退到文本匹配" 日志
     - 无 "尝试通用选择器" 日志
     - 无 "KB XPath 失败" 日志
     - 所有步骤直接使用 playbook 提供的 locator
     - 成功捕获 CRUD APIs
    
     3.3 端到端测试
    
     python -m module_discovery.run --project ecm-compute --module 用户管理 --stage all
    
     验证完整流水线成功。
    
     ---
     关键文件清单
    
     ┌──────────────────────────────────┬──────────┬────────────────────────────────────────────────────────────────────────────┐
     │               文件               │ 修改类型 │                                    说明                                    │
     ├──────────────────────────────────┼──────────┼────────────────────────────────────────────────────────────────────────────┤
     │ module_discovery/discover_ui.py  │ 增强     │ _do_create 返回更多字段详情build_playbook 传递完整信息                     │
     ├──────────────────────────────────┼──────────┼────────────────────────────────────────────────────────────────────────────┤
     │ module_discovery/form_filler.py  │ 增强     │ fill_multi_step_fields 返回详情MultiStepExecutor 新增 execute_with_details │
     ├──────────────────────────────────┼──────────┼────────────────────────────────────────────────────────────────────────────┤
     │ module_discovery/capture_apis.py │ 清理     │ 删除所有 _step_* 函数中的探测逻辑                                          │
     └──────────────────────────────────┴──────────┴────────────────────────────────────────────────────────────────────────────┘
    
     遗漏检查
    
     已覆盖的 Stage 2 函数
    
     ┌──────────────────────────────┬─────────────────────────────────────────────────────────────────────┐
     │             函数             │                              覆盖情况                               │
     ├──────────────────────────────┼─────────────────────────────────────────────────────────────────────┤
     │ _step_click_button           │ ✅ Phase 2.1                                                        │
     ├──────────────────────────────┼─────────────────────────────────────────────────────────────────────┤
     │ _step_wait_for_dialog        │ ✅ Phase 2.2                                                        │
     ├──────────────────────────────┼─────────────────────────────────────────────────────────────────────┤
     │ _step_fill_form              │ ✅ Phase 2.3                                                        │
     ├──────────────────────────────┼─────────────────────────────────────────────────────────────────────┤
     │ _step_find_row               │ ✅ 纯执行，无探测（find_data_row 内部分页是合理容错）               │
     ├──────────────────────────────┼─────────────────────────────────────────────────────────────────────┤
     │ _step_hover_dropdown         │ ✅ Phase 2.4                                                        │
     ├──────────────────────────────┼─────────────────────────────────────────────────────────────────────┤
     │ _step_click_dropdown_item    │ ✅ 已经是纯执行                                                     │
     ├──────────────────────────────┼─────────────────────────────────────────────────────────────────────┤
     │ _step_click_confirm_dialog   │ ✅ Phase 2.5                                                        │
     ├──────────────────────────────┼─────────────────────────────────────────────────────────────────────┤
     │ _step_assert_success         │ ⚠️ 需要在 Phase 2 中补充：删除默认值，要求 Stage 1 必须提供 locator │
     ├──────────────────────────────┼─────────────────────────────────────────────────────────────────────┤
     │ _step_assert_row_disappeared │ ✅ 已经是纯执行                                                     │
     ├──────────────────────────────┼─────────────────────────────────────────────────────────────────────┤
     │ _step_select_row_checkbox    │ ✅ Phase 2.6                                                        │
     └──────────────────────────────┴─────────────────────────────────────────────────────────────────────┘
    
     遗漏补充：_step_assert_success 清理
    
     async def _step_assert_success(page, step: dict):
         """步骤：验证操作成功（纯执行，无默认值）"""
         locator = step.get("playwright_locator")
    
         if not locator:
             LOG.warning(f"    assert_success 步骤缺少 locator（Stage 1 未提供），跳过验证")
             return
    
         try:
             await page.wait_for_selector(locator, state="visible", timeout=5000)
         except Exception:
             LOG.warning(f"    未检测到成功消息: {locator}")
    
     遗漏补充：_build_update_steps 和 _build_generic_steps
    
     这两个函数也存在同样的问题：
     - 硬编码 .el-dialog__wrapper 和 .el-message--success
     - button:has-text('{trigger}') 构造而非使用已验证 locator
     - _build_update_steps 的 fill_form 步骤直接传递原始 form_fields（无 kb_category、fill_rule）
    
     修改方式与 _build_create_steps 和 _build_dropdown_steps 相同模式，此处不重复展开。
    
     遗漏补充：Selector 跨上下文可移植性
    
     风险：Stage 1 在弹窗（dialog）中扫描表单，Stage 2 在创建页面（page-nav）中回放。CSS selector 如 div > .el-form-item:nth-of-type(9) .el-select 是基于 DOM
     位置的，只要表单结构一致就能跨上下文工作。
    
     缓解措施：
     - Stage 1 的 _do_create 已在 page-nav 模式下验证过这些 selector（因为 Stage 1 本身也在创建页面执行过操作）
     - 如果表单结构在 dialog 和 page-nav 之间不同，需要在 Stage 1 分别记录两套 selector
     - 当前用户管理模块是 page-nav 模式，不存在此问题
    
     ---
     风险与注意事项
    
     1. 向后兼容：旧 playbook 没有新增字段时，Stage 2 会报错（而非静默回退）。这是预期行为，强制用户重新运行 Stage 1。
     2. KB XPath 保留范围：
       - 保留：fill、select、first-option 步骤（选择下拉选项的标准流程）
       - 删除：expand 步骤的 KB XPath 回退（展开下拉框应该用 Stage 1 的 selector）
     3. _discover_first_option 保留：当 option_text 为空时，仍需要运行时读取第一个选项。这是因为 Stage 1 可能没有记录选项文本（选第一项时无需记录）。
     4. editable-check 保留：虽然理想情况下应该从 Stage 1 传入 is_editable，但这需要较大的改动。暂时保留 DOM 检测作为过渡，后续可优化。
    
     ---
     预期效果
    
     修改前：
     Stage 2 日志：
     - "回退到文本匹配: 创建用户"
     - "尝试通用选择器: button:has-text(...)"
     - "multi_step 失败: 系统角色 (el-select)"
     - "KB XPath 模式未匹配"
    
     修改后：
     Stage 2 日志：
     - "点击按钮: button:has-text('创 建 用 户')"
     - "填充: 账号 = autotest312345"
     - "selector 展开成功: 系统角色"
     - "选择选项: 普通用户"
     - "已填充 9/9 个字段"
    
     Phase 1: Stage 1 输出增强 ✅
    
     ┌──────┬──────────────────────────────────────────────┬────────────────────────────────┬──────┐
     │ 步骤 │                     修改                     │              文件              │ 状态 │
     ├──────┼──────────────────────────────────────────────┼────────────────────────────────┼──────┤
     │ 1    │ build_playbook() 函数                        │ discover_ui.py 末尾（~200 行） │ ✅   │
     ├──────┼──────────────────────────────────────────────┼────────────────────────────────┼──────┤
     │ 2    │ _do_create 增加 locator 返回                 │ discover_ui.py:1840-1865       │ ✅   │
     ├──────┼──────────────────────────────────────────────┼────────────────────────────────┼──────┤
     │ 3    │ _do_generic_operation 增加 dropdown 细节     │ discover_ui.py:2324-2360       │ ✅   │
     ├──────┼──────────────────────────────────────────────┼────────────────────────────────┼──────┤
     │ 4    │ scan_form_fields 为所有组件类型生成 selector │ form_filler.py:565-600         │ ✅   │
     ├──────┼──────────────────────────────────────────────┼────────────────────────────────┼──────┤
     │ 5    │ run_stage1 调用 build_playbook 并保存        │ run.py:244-252                 │ ✅   │
     └──────┴──────────────────────────────────────────────┴────────────────────────────────┴──────┘
    
     Phase 2: Stage 2 重构 ✅
    
     ┌──────┬───────────────────────────────────────────────┬─────────────────┬──────┐
     │ 步骤 │                     修改                      │      文件       │ 状态 │
     ├──────┼───────────────────────────────────────────────┼─────────────────┼──────┤
     │ 6    │ replay_from_playbook() + 11 个 _step_* 执行器 │ capture_apis.py │ ✅   │
     ├──────┼───────────────────────────────────────────────┼─────────────────┼──────┤
     │ 7    │ capture_all() 入口重写 + 旧 _replay_* 已删除  │ capture_apis.py │ ✅   │
     └──────┴───────────────────────────────────────────────┴─────────────────┴──────┘
    
     Phase 3: UI 脚本生成 ✅
    
     ┌──────┬──────────────────────────────────────┬────────────────────────────────────────────────┬──────┐
     │ 步骤 │                 修改                 │                      文件                      │ 状态 │
     ├──────┼──────────────────────────────────────┼────────────────────────────────────────────────┼──────┤
     │ 8-9  │ generate_ui_script.py + 数据文件生成 │ module_discovery/generate_ui_script.py（新建） │ ✅   │
     └──────┴──────────────────────────────────────┴────────────────────────────────────────────────┴──────┘
    
     Phase 4: 目录迁移 + 集成 ⏳
    
     ┌──────┬───────────────────────────────────────────────────────────────────────────────────┬────────────────────┬─────────────┐
     │ 步骤 │                                       修改                                        │        文件        │    状态     │
     ├──────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────┼─────────────┤
     │ 10   │ gen_test.py 输出路径 flows/ → scripts/<ver>/api/                                  │ gen_test.py:96-107 │ ✅          │
     ├──────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────┼─────────────┤
     │ 11   │ version.py 新增 scripts_dir_for()                                                 │ version.py:39-45   │ ✅          │
     ├──────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────┼─────────────┤
     │ 12   │ run_stage2 集成 generate_ui_script()                                              │ run.py:441-460     │ ✅          │
     ├──────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────┼─────────────┤
     │ 13   │ 创建 scripts/v1.0.0/{api,ui}/ 目录                                                │ 文件系统           │ ✅          │
     ├──────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────┼─────────────┤
     │ 14   │ 迁移 API 脚本 + 删除 flows/                                                       │ 文件系统           │ ✅          │
     ├──────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────┼─────────────┤
     │ 15   │ 更新代码引用 (run_suite.py, test_report.py, version.py, __init__.py, pytest 插件) │ 多文件             │ ⏳ 部分完成 │
     └──────┴───────────────────────────────────────────────────────────────────────────────────┴────────────────────┴─────────────┘
    
     剩余工作
    
     1. 修复编译失败的引用更新
    
     - module_discovery/__init__.py — flows/ → scripts/ （上次 Edit 失败，需要重新读取后修改）
     - module_discovery/version.py — 文件头注释中 flows/ → scripts/（上次 Edit 匹配失败）
     - module_discovery/run_parallel.py — flows/<version>/ 路径引用
     - module_discovery/run_with_version.py — flows/<version>/ 路径引用
    
     2. 编译验证
    
     python -m py_compile module_discovery/capture_apis.py
     python -m py_compile module_discovery/discover_ui.py
     python -m py_compile module_discovery/form_filler.py
     python -m py_compile module_discovery/generate_ui_script.py
     python -m py_compile module_discovery/gen_test.py
     python -m py_compile module_discovery/run.py
     python -m py_compile module_discovery/version.py
     python -m py_compile module_discovery/__init__.py
     python -m py_compile run_suite.py
    
     3. 运行单元测试
    
     python -m pytest tests/ -v -x --timeout=30
    
     4. 更新 memory
    
     - 更新 capture-pipeline-status.md 记录 Playbook 架构重构
    
     5. 更新 PROJECT_GUIDE.md（低优先级）
    
     - 目录结构图 flows/ → scripts/
     - 新增 playbook.json 和 UI 脚本说明
    
     关键文件清单
    
     ┌────────────────────────────────────────┬──────────┬─────────────────────────────────────────────────────────────────────────────┐
     │                  文件                  │ 变更类型 │                                    说明                                     │
     ├────────────────────────────────────────┼──────────┼─────────────────────────────────────────────────────────────────────────────┤
     │ module_discovery/discover_ui.py        │ 修改     │ build_playbook() + _do_create locator + _do_generic_operation dropdown 细节 │
     ├────────────────────────────────────────┼──────────┼─────────────────────────────────────────────────────────────────────────────┤
     │ module_discovery/form_filler.py        │ 修改     │ scan_form_fields JS 为所有组件类型生成 selector                             │
     ├────────────────────────────────────────┼──────────┼─────────────────────────────────────────────────────────────────────────────┤
     │ module_discovery/capture_apis.py       │ 重写     │ playbook 驱动 + replay_from_playbook() + 11 个步骤执行器                    │
     ├────────────────────────────────────────┼──────────┼─────────────────────────────────────────────────────────────────────────────┤
     │ module_discovery/generate_ui_script.py │ 新建     │ UI 脚本 + 数据文件生成器                                                    │
     ├────────────────────────────────────────┼──────────┼─────────────────────────────────────────────────────────────────────────────┤
     │ module_discovery/gen_test.py           │ 修改     │ save_script_to_file 输出到 scripts/<ver>/api/                               │
     ├────────────────────────────────────────┼──────────┼─────────────────────────────────────────────────────────────────────────────┤
     │ module_discovery/version.py            │ 修改     │ 新增 scripts_dir_for()                                                      │
     ├────────────────────────────────────────┼──────────┼─────────────────────────────────────────────────────────────────────────────┤
     │ module_discovery/run.py                │ 修改     │ run_stage1 保存 playbook + run_stage2 生成 UI 脚本                          │
     ├────────────────────────────────────────┼──────────┼─────────────────────────────────────────────────────────────────────────────┤
     │ run_suite.py                           │ 修改     │ 扫描路径 flows/ → scripts/*/api/                                            │
     ├────────────────────────────────────────┼──────────┼─────────────────────────────────────────────────────────────────────────────┤
     │ lib/test_report.py                     │ 修改     │ 示例路径更新                                                                │
     └────────────────────────────────────────┴──────────┴─────────────────────────────────────────────────────────────────────────────┘
    
     验证方案
    
     端到端验证
    
     python -m module_discovery.run --project ecm-compute \
         --url "/estack/web/estack/user-center/user-manage/user" \
         --module "用户管理" --stage 1